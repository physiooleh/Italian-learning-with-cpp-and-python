// Here i want to drill some queue and map skills with AI help

#include <iostream>
#include <string>
#include <queue>
#include <map>

using namespace std;

int main() {
	map<string, string> regions;
	queue<string> cities;

	string comando;
	cout << "Write 'aiuto' to know the commands \n";

	while (true) {
		cout << "\n> ";
		getline(cin, comando);
		
		if (comando == "aiuto") {
		    cout << "Basilicata \n";
			cout << "Abruzzo \n";
			cout << "Mamma mia, basta \n";
		}
		else if (comando == "inserisci regione") {
			string name, info;
			cout << "Write the region name \n";
			getline(cin, name);
			cout << "Say something about the region \n";
			regions[name] = info;
			cout << "Region is added \n";
		}
		else if (comando == "Mostra tutti i regioni \n") {
			cout << "List of regions \n";
			for (auto& r : regions) {
				cout <<"- " <<r.first << endl;
			}
		}
		else if (comando == "Mostra regione") {
			string name;
			cout << "Write the name of region \n";
			getline(cin, name);
			if (regions.find(name) != regions.end()) {
				cout << regions[name] << endl;
			} else {
			(comando == "Mamma mia, basta \n");
			cout << "Ciao :) \n";
			break;
			}
		}
	}
}
